import{_ as a}from"./plugin-vue_export-helper-c27b6911.js";import{o as s,c as o,e as n,a as e,b as t}from"./app-9374d132.js";const i="/assets/images/042CDA8A-7327-4E97-A135-ABA3D3BDAF94.png",l={},r=n(`<h1 id="debezium" tabindex="-1"><a class="header-anchor" href="#debezium" aria-hidden="true">#</a> debezium</h1><h2 id="配置示例" tabindex="-1"><a class="header-anchor" href="#配置示例" aria-hidden="true">#</a> 配置示例</h2><p>为了方便起见，先编辑一个文件 mysql10.250.43.107-connector.json：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>{
    &quot;name&quot;:&quot;mysql10_250_43_107-connector&quot;,
    &quot;config&quot;:{
        &quot;connector.class&quot;:&quot;io.debezium.connector.mysql.MySqlConnector&quot;,
        &quot;database.hostname&quot;:&quot;10.250.43.107&quot;,
        &quot;database.port&quot;:&quot;3306&quot;,
        &quot;database.user&quot;:&quot;root&quot;,
        &quot;database.password&quot;:&quot;qmPeZR38bffnG1KsRafb&quot;,
        &quot;database.server.id&quot;:&quot;2385205150&quot;,
        &quot;database.server.name&quot;:&quot;mysql10_250_43_107&quot;,
        &quot;database.history.kafka.bootstrap.servers&quot;:&quot;hwtest-data1:9092&quot;,
        &quot;database.history.kafka.topic&quot;:&quot;ods_fin_settlement_fin_cs_withdraw_kafka&quot;,
        &quot;include.schema.changes&quot;:&quot;true&quot;,
        &quot;database.whitelist&quot;:&quot;fin_settlement&quot;,
        &quot;table.whitlelist&quot;:&quot;fin_cs_withdraw&quot;
    }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><figure><img src="`+i+`" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><ul><li>通过 Http Post 请求新增 connector 连接器实例：</li></ul><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>curl -X POST -H &quot;Content-Type:application/json&quot; --data @mysql10.250.43.107-connector.json http://127.0.0.1:8083/connectors
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li>通过 Http Post 请求删除 connector 连接器实例：</li></ul><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>curl -X DELETE -H &quot;Content-Type:application/json&quot; --data @mysql10.250.43.107-connector.json http://127.0.0.1:8083/connectors/mysql10_250_43_107-connector
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li>查看新增的连接器实例：</li></ul><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>curl http://127.0.0.1:8083/connectors 
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li>查看连接器实例运行状态：</li></ul><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>curl http://127.0.0.1:8083/connectors/mysql10_250_43_107-connector/status
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="kafka-connet-sql-server从指定位置修复" tabindex="-1"><a class="header-anchor" href="#kafka-connet-sql-server从指定位置修复" aria-hidden="true">#</a> kafka connet sql server从指定位置修复</h2>`,14),c=e("p",{"transaction_id:null,event_serial_no:2,commit_lsn:000F9F22:00001660:0003,change_lsn:000F9F22:00001220:0003":""},[t("发送一条数据到connect-offsets"),e("br"),t(" key："),e("br"),t(' ["sqlserver-cdc-source-AIS20201114225525",{"server":"AIS20201114225525"}]'),e("br"),t(" value：")],-1),d=n('<h2 id="snapshot-mode参数" tabindex="-1"><a class="header-anchor" href="#snapshot-mode参数" aria-hidden="true">#</a> snapshot.mode参数</h2><ul><li><p>snapshot.mode<br> Debezium 支持五种模式:</p><ol><li>initial ：默认模式，在没有找到 offset 时(记录在 Kafka topic 的 connect-offsets 中，Kafka connect 框架维护)，做一次 snapshot——遍历有 SELECT 权限的表，收集列名，并且将每个表的所有行 select 出来写入 Kafka；</li><li>when_needed: 跟 initial 类似，只是增加了一种情况，当记录的 offset 对应的 binlog 位置已经在 MySQL 服务端被 purge 了时，就重新做一个 snapshot。</li><li>never: 不做 snapshot，也就是不拿所有表的列名，也不导出表数据到 Kafka，这个模式下，要求从最开头消费 binlog，以获取完整的 DDL 信息，MySQL 服务端的 binlog 不能被 purge 过，否则由于 DML binlog 里只有 database name、table name、column type 却没有 column name，Debezium 会报错 Encountered change event for table some_db.some_table whose schema isn&#39;t known to this connector；</li><li>schema_only: 这种情况下会拿所有表的列名信息，但不会导出表数据到 Kafka，而且只从 Debezium 启动那刻起的 binlog 末尾开始消费，所以很适合不关心历史数据，只关心最近变更的场合。</li><li>schema_only_recovery: 在 Debezium 的 schema_only 模式出错时，用这个模式恢复，一般不会用到。</li></ol></li><li><p>snapshot.locking.mode<br> 设置为 “none” 是为了避免获取表的元信息时锁表（要么是 RELOAD 权限用 flush tables with read lock，要么是 LOCK TABLES 权限锁单个表），此时要求 Debezium 启动或者重启时没有 DDL 语句执行，否则 Debezium 抓取到的元信息跟并发执行的 DML 之间不一致。<br> sqlserver 只支持<br> initial_only, initial, schema_only</p></li><li><p>initial: Takes a snapshot of structure and data of captured tables; useful if topics should be populated with a complete representation of the data from the captured tables.全量+增量</p></li><li><p>initial_only: Takes a snapshot of structure and data like initial but instead does not transition into streaming changes once the snapshot has completed.全量</p></li><li><p>schema_only: Takes a snapshot of the structure of captured tables only; useful if only changes happening from now onwards should be propagated to topics.从启动消费</p></li></ul><h3 id="默认都是-initial" tabindex="-1"><a class="header-anchor" href="#默认都是-initial" aria-hidden="true">#</a> 默认都是 initial</h3><h3 id="mysql" tabindex="-1"><a class="header-anchor" href="#mysql" aria-hidden="true">#</a> mysql</h3><p>Specifies the criteria for running a snapshot when the connector starts. Possible settings are:</p><ul><li><p>initial - the connector runs a snapshot only when no offsets have been recorded for the logical server name.</p></li><li><p>initial_only - the connector runs a snapshot only when no offsets have been recorded for the logical server name and then stops; i.e. it will not read change events from the binlog.</p></li><li><p>when_needed - the connector runs a snapshot upon startup whenever it deems it necessary. That is, when no offsets are available, or when a previously recorded offset specifies a binlog location or GTID that is not available in the server.</p></li><li><p>never - the connector never uses snapshots. Upon first startup with a logical server name, the connector reads from the beginning of the binlog. Configure this behavior with care. It is valid only when the binlog is guaranteed to contain the entire history of the database.</p></li><li><p>schema_only - the connector runs a snapshot of the schemas and not the data. This setting is useful when you do not need the topics to contain a consistent snapshot of the data but need them to have only the changes since the connector was started.</p></li><li><p>schema_only_recovery - this is a recovery setting for a connector that has already been capturing changes. When you restart the connector, this setting enables recovery of a corrupted or lost database schema history topic. You might set it periodically to &quot;clean up&quot; a database schema history topic that has been growing unexpectedly. Database schema history topics require infinite retention.</p></li></ul>',6),u=[r,c,d];function h(m,p){return s(),o("div",null,u)}const f=a(l,[["render",h],["__file","debezium.html.vue"]]);export{f as default};
